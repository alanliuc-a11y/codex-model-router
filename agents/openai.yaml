interface:
  display_name: "模型路由器（高额度档）"
  short_description: "适用于高额度方案：按完整规则推荐模型与推理强度，不自动切换"
  default_prompt: "Use $model-router to recommend the most efficient model and reasoning effort for this task without executing it or changing speed settings."
policy:
  allow_implicit_invocation: true
